/*package fiveinarow;
package Pzyber.Loki;

public class JimmyAI extends Player {

    private Loki loki;

    public AIPlayer(int id, Game game) {
        super(id, game);
        loki = new Loki("Loki", game.getWidth(), game.getHeight());
    }

    @Override
    public void playRound(){
        int[] move = Loki.calculateMove(game.GetBoard()), ID);
        game.SetTile(move[0], move[1], ID);
        game.nextPlayer();
    }
}
*/